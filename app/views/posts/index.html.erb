<% provide(:title, 'Homepage') %>
<% provide(:description, 'Watch the Awesome Videos ever that you are never watch before.') %>

<% provide(:url, 'http://awesomepv.com') %>
<% provide(:thumbnail, 'https://www.dropbox.com/s/0kqxekzzb2i65gg/logo.png') %>

<div class="col-md-4 col-md-offset-4">
    <div class="form-group">
    <%= form_tag(posts_path, method: "get") do %>
        <%= text_field_tag(:q, params[:search], class: 'form-control', placeholder: 'Search any video you want here') %>
    <% end %>
    </div>
</div>

<section class="grid-wrap">
  <ul class="grid swipe-rotate" id="grid">
    <% if @posts.empty? %>
      <h1>No video found!</h1>
      <%= link_to "Back", root_path, :class => 'btn btn-primary btn-md' %>
    <% else %>
      <li class="title-box">
        <a href="https://affiliates.mozilla.org/referral/56831/"><img src="https://affiliates.mozilla.org/media/uploads/banners/066ac34f247ac5121eed1e9cefb0383569c62903.png" alt=""></a>
      </li>
      <% @posts.each do |post| %>
      <li>
        <a href="<%= post_path(post) %>"><img src="http://i3.ytimg.com/vi/<%= post.youtube_id %>/hqdefault.jpg" alt="<%= post.title %>">
          <h3><%= post.title %><small> | <%= post.view_count %> views</small></h3>
        </a>
      </li>
      <% end %>
    <% end %>
  </ul>
	<%= will_paginate @posts, renderer: BootstrapPagination::Rails %>
</section>
